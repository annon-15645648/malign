# malign

This repo contains a single zip file which is annonymized version of the malign project repo elsewhere. It contains links to used datasets, the whole codebase and results from the latest experiment run. It's fully reproducible by downloading the datasets, installing all requirements with poetry and running the run_all bash script.
